<section class="page">
    <h1>{{labels.blog.title}}</h1>
    <p>{{ labels.blog.subtitle }}</p>
    <ul>
        {{#posts}}
            <li>
                <time>{{formatDate created_at}}</time> <a href="{{getLink .}}">{{title}}</a>
            </li>
        {{/posts}}
    </ul>
</section>
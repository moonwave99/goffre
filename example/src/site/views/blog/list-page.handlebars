<section class="page">
    <header>
        <h1>{{labels.blog.title}}</h1>
        {{#pagination}}
        <p>Page <strong>{{page}}</strong> of <strong>{{total}}</strong></p>
        {{/pagination}}
    </header>
    {{> blog/post-list}}
    <p class="pagination">
        {{#pagination}}
            {{#prev}}
            <a href="{{getPrevPageLink .}}">Newer posts</a>
            {{/prev}}
            {{#next}}
            <a href="/blog/page/{{.}}">Older posts</a>
            {{/next}}
        {{/pagination}}
    </p>
</section>